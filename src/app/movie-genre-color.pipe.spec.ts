import { MovieGenreColorPipe } from './movie-genre-color.pipe';

describe('MovieGenreColorPipe', () => {
  it('create an instance', () => {
    const pipe = new MovieGenreColorPipe();
    expect(pipe).toBeTruthy();
  });
});
